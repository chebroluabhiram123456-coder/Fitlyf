# This is the FIX. It gives the build process 4GB of RAM to work with,
# which is more than enough to prevent the "Java heap space" error.
org.gradle.jvmargs=-Xmx4G

# These are standard properties for a Flutter project.
android.useAndroidX=true
android.enableJetifier=true
